import packageJson from "../models/new-sub.js";


const newScript = async (output) => {
  
  // const packageJson = await readFile('../models/new-sub.json')
  // console.log(packageJson);
  console.log(output);
  console.log('Hello World!');

};

export default newScript;

